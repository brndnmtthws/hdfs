#!/bin/bash

if [ -z "$PORT0" ] ; then
  PORT0="8765"
fi

exec `if [ -z "$JAVA_HOME" ]; then echo java; else echo $JAVA_HOME/bin/java; fi` -cp lib/*.jar -Dmesos.conf.path=etc/hadoop/mesos-site.xml -Dmesos.hdfs.config.server.port=$PORT0 org.apache.mesos.hdfs.Main
